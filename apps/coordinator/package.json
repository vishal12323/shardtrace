{
  "name": "coordinator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@shardtrace/inference-engine": "workspace:*",
    "@shardtrace/proof-engine": "workspace:*",
    "@shardtrace/shard-engine": "workspace:*",
    "@shardtrace/shared": "workspace:*",
    "ethers": "^6.13.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.8.2"
  }
}
